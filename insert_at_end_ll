class Node:
    def __init__(self,data):
        self.data=data
        self.next=None

def print_list(head):
    temp=head
    while temp:
        print(temp.data,end=" -> ")
        temp=temp.next
    print()

head=Node(10)
head.next=Node(20)

new_node=Node(30)

temp=head
while temp.next:
    temp=temp.next

temp.next=new_node

print_list(head)
